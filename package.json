{
  "name": "enmanuel-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Enmanuel Torres — personal portfolio",
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ./style.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./style.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
